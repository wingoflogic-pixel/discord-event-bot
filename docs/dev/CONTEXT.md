# EventMasterBot（汎用イベント出欠Bot）

Discord 上でイベントの出欠を募り、回答状況の集計・未回答リマインド・番号割り当てを行う Bot。
当初は特定コミュニティ「ちょいえむ」専用だったが、任意のコミュニティが自己ホストで使える汎用ツールへ拡張中。1 デプロイで**複数の Discord サーバー**を扱う（マルチサーバー）。ただしテナント分離（サーバーごとの認可境界）は持たず、単一の運用者が `ADMIN_TOKEN` で全サーバーを管理する。言語は日本語固定、時刻は JST 固定。

## Language

**Server（サーバー / Guild）**:
Bot が参加している 1 つの Discord サーバー。管理画面の**最上位のスコープ**で、Notification（通知）と Segment はいずれか 1 つの Server に属する（`guild_id` で直接紐づく）。管理操作はまず Server を選び、以降はその配下に限定される。サーバー一覧と表示名は Discord API（bot の参加サーバー）から取得し、DB には永続化しない。Discord 上の呼称は「Guild」だが、UI 表記は「サーバー」に統一する。
_Avoid_: ギルド（UI 表記）, テナント（認可境界は持たない）, グループ

> **Event（廃止）**: かつて Server と Notification の間に「出欠管理を束ねるグループ（例: 土曜定例）」という層を置いていたが、1 サーバー＝実質 1 プログラムの運用では空フォルダとなり混乱を招くため廃止した。通知は Server に直接ぶら下がる。経緯は ADR を参照。

**Segment（区分）**:
設定可能なメンバー区分（例: キャスト / スタッフ）。1 つの Server に属する（`guild_id` で直接紐づく）。コミュニティが自由に定義する。Member が所属し、休止状態（アクティブ / 休止中）は**区分ごと**に持つ。@メンション用の Discord ロール（または `@everyone`）を任意で紐付けられる。**ロールを紐付けた区分は「ロール管理区分」**になり、メンバーをそのロール保有者から完全同期する（手動追加/除外は不可・休止は可、ADR 0009）。未設定なら手動管理。
_Avoid_: ロール（Discord のものと紛らわしい）, 役割, カテゴリ

**Notification（通知）**:
1 つの Server の配下にある独立したトラック（`guild_id` で直接紐づく）。対象は 1 つの Segment。投稿チャンネル・スケジュール(RRULE)・募集/リマインド日数・ノルマ設定・番号割り当て設定に加え、投稿の体裁（**回答要否**・**メンション方法**・**カスタムメッセージ**）も所有する。対象者＝その Segment のアクティブな Member。出欠タリーは Notification ごとに分かれる。「単なる配信設定」ではなく出欠管理の単位そのもの。
_Avoid_: track, 配信設定, チャンネル設定

**回答要否（通知のみ / 回答不要）**:
recurring な Notification が「出欠回答を集めるか否か」の設定。既定は**回答あり**（参加/不参加/未定ボタン付きで募集）。**回答不要（通知のみ）**にすると、ボタンの無い開催告知をチャンネルへ投稿するだけになり、Response を一切集めない。回答に依存する機能（未回答/未定リマインド・Quota・Assignment）は対象外になる。「基本出席が前提」の運用（例: 特定の必須出勤日）向け。oneoff は常に回答あり。
_Avoid_: 告知通知（タイプ名と紛らわしい）, ROM通知

**メンション方法（mention_mode）**:
Recruitment／告知が対象者をどう名指すか。3 値の排他選択: **なし** / **ロール**（Segment のロール、または `@everyone`）/ **バイネーム**（Segment のアクティブ Member を `@ユーザー` で個別に列挙）。バイネームは少人数の区分向けで、メッセージ長上限に収まらない分は「ほか N 名」と省略する。全 Notification で選択可能。
_Avoid_: 通知先, 宛先設定

**カスタムメッセージ（見出し・本文）**:
Notification ごとに設定する投稿文面。**見出し**（必須・1 行）と**本文**（任意・複数行）の 2 つから成る。チャンネルへの募集／告知投稿の顔として使い、日時行と回答ボタンはシステムが自動付加する。DM・Quota・Assignment などの機能的通知には適用しない。
_Avoid_: テンプレート, 定型文

**次回の開催日（繰り返しの起点）**:
隔週の開催週パリティを決めるための起点となる開催日。内部的には `notifications.anchor_date`（dtstart）として保存するが、UI では**隔週選択時のみ**「次にこの通知で開催する日」候補として提示する。
_Avoid_: 基準日（廃語・専門的で伝わらない）, anchor_date（内部実装語）

**送信時刻（通知ごと・任意）**:
その Notification の cron 駆動メッセージ（募集／未回答・未定リマインド／締切告知／ノルマ）を「1 日のうち何時に送るか」。通知ごとに時単位で設定し、未設定は従来挙動の JST21:00。**開催の開始時刻（`start_time`）とは別物**で、こちらは“通知を送る時刻”。実装上 cron を高頻度起動にし、現在の JST 時が送信時刻に一致する通知だけ日次パスを走らせる。①の締切後の即時変更通知やボタン応答は cron 非経由のため対象外。
_Avoid_: 開始時刻（開催の時刻と紛らわしい）, 配信時刻

**Occurrence（開催回）**:
ある Notification の 1 回ぶんの開催／募集。日付を持ち、Member の Response が紐づく単位。Notification の募集が走るタイミングで実体（行）として生成される。単発の Notification はちょうど 1 つ持つ。
_Avoid_: instance, session, 回

**Response（回答）**:
ある Occurrence に対して 1 人の Member が示した出欠の意思。値は 参加 / 不参加 / 未定 で固定。Member が押す「回答ボタン」を口語で「出勤ボタン」と呼ぶことがあるが、当日の打刻（勤怠）ではなく事前の出欠表明である。
_Avoid_: 出欠記録, attendance（集計結果と紛らわしいため）, 出勤ボタン（口語。打刻＝勤怠と紛らわしいので「回答ボタン／Response」と呼ぶ）

**回答締切（確定日時・任意）**:
ある Occurrence について「これ以降は Response を変更しないでほしい」境界として運用者が任意で設定する締切。Notification に「開催開始の N 時間前」というオフセットで 1 つ持たせ、その Notification の全 Occurrence に自動適用する（未設定＝締切なし）。締切を過ぎても**回答ボタンはロックしない**（Response の変更は技術的に可能なまま）。締切が来たらメンバーに締切を告知し、締切後に Response が変更されたら（**未回答→回答の初回も含む**）検知して、運用者が指定したチャンネルへ通知する（**メンションは付けない**）。さらに当該 Response に「締切後の変更」の印を残し、回答履歴で締切後変更か否かを識別できる。確定後の変更を運用者が見逃さないための仕組み。単発の「確定（候補日確定・`decided_occurrence_id`）」とは無関係の別概念。
_Avoid_: 確定（単発の候補日確定と紛らわしい）, ロック（回答ボタンは固定しない）, 〆切（表記ゆれ）, 監督ロール（①はロール通知ではなくチャンネル投稿に決定）

**Member**:
出欠管理の対象としてマスタ登録された人物。1 人の Member は複数の Segment に所属しうる。リマインドやノルマ通知の宛先になる。Discord ユーザー一般（User）とは区別する。
_Avoid_: キャスト, cast, スタッフ

**User**:
Discord 上のユーザー一般。Member は「マスタ登録された User」である点で異なる。

**Recruitment（募集）**:
Occurrence の到来前に、Notification の投稿チャンネルへ出欠ボタン付きで投稿される告知。対象 Segment の Discord ロールへの @メンションを伴うことがある。

**Reminder（リマインド）**:
Occurrence の到来前、対象 Segment のアクティブ Member へ個別 DM で回答を促す通知。**母集団の異なる 2 種を区別する**:
- **未回答リマインド**: まだ一度も回答していない Member（未回答）が対象。開催の N 日前から当日まで毎日送る。
- **未定リマインド**: 「未定」と回答した Member が対象。開催の N 日前に 1 回だけ「参加/不参加の確定」を促す。

「未回答（一度も押していない）」と「未定（"未定"で回答済み）」は別の状態であり、対象も送るタイミングも分ける。
_Avoid_: 督促（Quota と紛らわしい）, 催促

**Quota（ノルマ）**:
Notification ごとに任意で有効化できる参加間隔の督促。前回「参加」から設定日数を超えた、対象 Segment のアクティブ Member へ DM で参加を促す。既定は無効。
_Avoid_: 義務, obligation

**Assignment（番号割り当て）**:
Notification ごとに任意で有効化できる機能。ある Occurrence で「参加」と回答した Member へ、連番 1..N をランダムに重複なく割り当てる。管理者が手動実行し、既存の番号は維持して新規参加者にのみ空き番号を振る（安定割り当て）。割り当て結果は通知のチャンネルへ一覧で公開投稿する。番号は個室・座席等の外部割り当てに使う。既定は無効。
_Avoid_: 抽選, ロット, キャスト番号（汎用名は Assignment／割り当て番号）
