# タスクリスト（discord-event-bot）

開発・配布・運用の残タスクを一元管理する内部ドキュメント。**配布版（public `discord-event-bot`）には含めない**（他の dev ドキュメントと同様に除外する。→ R-5）。

- 最終更新: 2026-06-20
- 対象バージョン: v7.1.0
- 関連: [dev-and-release-flow.md](dev-and-release-flow.md) ／ [distribution-and-environments.html](distribution-and-environments.html) ／ [adr/](adr)

## 凡例

- 状態: 🔲未着手 ／ 🔄進行中 ／ ✅完了 ／ ⏸保留 ／ ❓要調査・要決定
- 担当: **CC**（Claude Code で実施可）／ **あなた**（Cloudflare/Discord/GitHub のダッシュボード等、CC 不可）／ **共同**
- 各タスクは `ID` で相互参照する。依存は「→ 前提タスクID」で示す。

## メンテ方法

1. 着手したら状態を 🔄、終わったら ✅ にする。
2. 新タスクは該当セクションに `ID` を採番して追記（ID は使い回さない）。
3. 完了タスクは消さずに「9. 完了済み（記録）」へ移すか、状態を ✅ にして残す（履歴になる）。
4. 決定が必要な論点は「8. オープンな決定事項」に集約し、決まったら本文へ反映。

---

## 0. 現在地（要約）

- **配布・更新モデルは案B（ユーザーが Fork → Cloudflare Workers Builds に接続、更新は純正「Sync fork」）を採用と決定**。**実機スモークテスト（B-1）完了（2026-06-20・staging）＝D1自動作成＋再デプロイでのデータ保全を実機検証済み**。検証中に**deploy順バグ**（migrations先で未作成DBに失敗）を発見し「deploy→migrations順／`database_id`省略」へ修正（staging・配布側ともコミット済み）。残るは内部explainer整合（B-6）と公開フロー（R-1〜）。
- **②検証(staging) は Workers Builds で構築済み**（Worker `discord-event-bot-staging`／D1 `choiemu-event-bot-db-staging`／Branch control=staging固定・非本番ブランチビルド無効）＝**P-1完了**。
- **`main` へ反映済み（2026-06-20）**: feature ブランチ `docs/github-account-and-distribution-guide` を FF マージし `origin/main` に push 完了（R-1/R-2）。旧 `wrangler.toml`（実 `database_id f6ff753c…`＝本番D1直撃の地雷）は除去確認済み、`wrangler.jsonc`(id省略)＋deploy順修正 deploy が main 上に存在。
- **公開リポジトリ `discord-event-bot` へ反映済み（2026-06-20）**: `public/main` に `16296b0 release: 案B配布モデル＋deploy順修正を公開版へ反映` を push 完了（R-3）。`docs/dev/` `.claude/` `CLAUDE.md` は除外確認済み（R-5）。
- **v7.1.0 リリース完了（2026-06-20）**: package.json を `7.1.0` に更新（MINOR・DB 変更なし）、GitHub Release [v7.1.0](https://github.com/taki98029/discord-event-bot/releases/tag/v7.1.0) を公開リポジトリに作成（R-4）。
- **②検証(staging) が完全稼働（2026-06-20）**: テスト用 Discord アプリ作成＋4シークレット投入＋ Interaction Endpoint URL 設定＋スラッシュコマンド登録まで完了（P-6 ✅／P-4 staging 分 ✅）。
- **③本番(prod) も Workers Builds 化完了（2026-06-20・P-2/P-3/P-4(prod) ✅）**: 本番 Worker `discord-event-bot` を `taki98029/event-master-bot` の main に Workers Builds で接続成功。Deploy command を `npm run deploy` に上書き、Production branch=main、非本番ビルド無効。**最重大成果**: Cloudflare の auto-provisioning が「同名既存 D1 を再利用」する挙動を実機で確認（接続後の D1 binding UUID `f6ff753c-bf29-4eb5-83ec-317631743cfc` が本番既存 D1 と完全一致＝本番データを保持して移行成功）。詳細は [ADR 0011 追補2](adr/0011-distribution-and-update-model.md)。リスク①②も「対応済み」へ更新済み（V-2/V-3 ✅）。
- **R-6 リポジトリ名整合確認＋修正完了（2026-06-20）**: 全主要文書を Workflow で並列調査し、`src/discord/rest.ts` と `scripts/register-commands.js` の `USER_AGENT` で GitHub URL のオーナー名抜け＋古い version（`7.0.0`）を発見。`https://github.com/taki98029/discord-event-bot, 7.1.0` に修正済み。配布物のため public-release への伝播必要。
- **残タスク**: P-5（`deploy:cli` 撤去＝しばらく観察後）、B-6（§2/§3 の図表全面改訂のみ＝§1/§4/§5 と冒頭バナーは整合済み）、Q-1（DBバックアップ運用の体系化）。
- リポジトリ: 開発=`taki98029/event-master-bot`(private)、配布=`taki98029/discord-event-bot`(public)。

---

## 1. 配布・更新モデル（案B）★いま最優先

| ID | 状態 | 担当 | タスク | 依存・備考 |
|----|------|------|--------|-----------|
| B-0 | ✅ | CC | 案Bの成立を一次情報で検証（fork→接続→D1自動生成→Sync fork更新→再デプロイでD1再利用） | 文書上はすべて成立。出典は [distribution-and-environments.html](distribution-and-environments.html) 末尾／Cloudflare・GitHub公式 |
| B-1 | ✅ | あなた＋CC | **実機スモークテスト完了（2026-06-20・staging）**: ①初回デプロイで D1 自動作成＋migrations 9本適用 ②再デプロイで**同じD1を継承（`env.DB (inherited)`）・捨てデータ保持**を実機確認。③検証中に**deploy順バグを発見→修正**（`deploy` を「deploy→migrations」順／`database_id` 省略へ）。Sync fork ボタン自体の目視は R-3 公開後でOK | push→再ビルド＝Sync fork と同機構 |
| B-2 | ✅ | あなた＋CC | 画面ラベル確認済み（Import a repository／Get started／Deploy command／Variables and Secrets／Branch control）。**発見**: 作成ウィザードに**ブランチ選択が無く**、リポジトリの**既定ブランチ＝本番ブランチ**になる。利用者は fork の `main` が既定＝正しいので setup.html は変更不要（検証では一時的に既定を staging にして対応） | → B-1 と同時 |
| B-3 | ✅ | CC | **setup.html を案Bに書き換え済み**: STEP 3 を「Fork → Cloudflare に Import → Deploy command を `npm run deploy` → デプロイ（D1自動生成）→ シークレット4つ → URL控え」に。更新節・STEP0 文言も実態へ。JS参照ID（v_pk 等）は保持。**画面ラベルの最終確認は B-1 で**（公式手順ベースで実装済み）|
| B-4 | ✅ | CC | **README / ADR 0011（追補）/ ADR 0012 / dev-and-release-flow.md / .claude/rules / wrangler.jsonc / .dev.vars.example を案Bに整合済み**。Deploy ボタン参照を一掃（ADR 0011 本文は歴史的記録として残し冒頭注記＋追補で上書き）| |
| B-5 | ✅ | CC | **ADR 0011 に追補**：Deploy ボタン（clone・Sync fork不可）を却下し案Bを採る理由・データ保全の仕組み・却下案（独自Action／再実行）を記録 | |
| B-6 | ✅ | CC | **完了（2026-06-20）**: distribution-and-environments.html §1〜§5 全面整合。Workflow 5並列で監査 → 冒頭 callout を good に・§1 figcaption・§2 末尾 lead・§3 全面（リード・アカウント表・SVG ②ブロック・SVG figcaption・callout good 本文・h3 段落）・「空化」→「省略」統一・「移行中」→「移行完了」を整合済み。あわせて全文書間の整合性監査で発見した不整合 19 件を修正（README.md コマンド一覧／src/interactions/index.ts:284 の `/resume` 文言／ui/index.html コマンド説明／setup.html Install Link 案内・PUBLIC_KEY エラー Troubleshooting・初回ビルド空コミット案内・/help /manage 利用案内／dev-and-release-flow.md コマンド一覧・同名 D1 再利用補足／IMPLEMENTATION-CONTRACT.md v7.1.0 コマンド再編注記／ADR 0009 の /pause /resume 言及）。typecheck・vitest 全 87 テスト通過。 | 内部explainer |
| B-7 | ✅ | CC | **敵対的検証ワークフロー完了**（verdict: fix-then-ship・ブロッカー無し）。アンカー切れ2件(major)＋setup.html の画面ラベル精度・更新依存注記・進捗バー初期値・データ保全のトラブル導線(minor/nit)を反映。検証中に見つかった**既存リンク不具合**（ADR 0006 リンク先・`.dev.vars` 安全モデルのアンカー）も修正 | Task wmsh73ie4 |

---

## 2. リリース／公開フロー（ブランチ → main → 配布）

| ID | 状態 | 担当 | タスク | 依存・備考 |
|----|------|------|--------|-----------|
| R-1 | ✅ | 共同 | **完了（2026-06-20・FF マージ）**: feature `docs/github-account-and-distribution-guide`（`e22950c`）を `main` に FF マージ。地雷だった旧 `wrangler.toml` を除去、`wrangler.jsonc`(id省略)＋修正deploy順 を main に反映。 | → B-1完了済 |
| R-2 | ✅ | あなた | **完了（2026-06-20）**: `main` を `origin`(private `event-master-bot`) に push 済み（`d118730..e22950c`）。 | → R-1 |
| R-3 | ✅ | 共同 | **完了（2026-06-20）**: `public-release` ブランチで main のツリーを取り込み → `docs/dev/` `.claude/` `CLAUDE.md` を除外 → `release: 案B配布モデル＋deploy順修正を公開版へ反映`（`16296b0`）を `public/main` に push。公開リポジトリは `taki98029/discord-event-bot` の main が最新。 | → R-2。公開リポジトリが Fork/Workers Builds の動力源 |
| R-4 | ✅ | 共同 | **完了（2026-06-20）**: version を `7.0.1` → `7.1.0`（MINOR）に更新。main・public/main 両方に反映済み。GitHub Release [v7.1.0](https://github.com/taki98029/discord-event-bot/releases/tag/v7.1.0) を公開リポジトリに作成（日本語リリースノート・DB変更なしを明記）。 | リリース手順（dev-and-release-flow.md）|
| R-5 | ✅ | CC | **確認済み（2026-06-20・R-3 実施時）**: 過去の除外パターン（47bde53）が `docs/dev/` 配下を一括除外する方針のため、本ファイル含む新規追加（`docs/dev/タスクリスト.md` / `dev-and-release-flow.md` / `distribution-and-environments.html` / ADR 0011/0012）は自動的に除外。同様に `.claude/` `CLAUDE.md` も除外。R-3 のコミット差分で漏れ無しを実機確認した。 | 公開キュレーションの仕組みを確認 |
| R-6 | ✅ | CC | **完了（2026-06-20）**: 全主要文書（README/setup.html/CLAUDE.md/dev-and-release-flow.md/ADR 0011/0012/.claude/rules/package.json/wrangler.jsonc）を Workflow 並列調査。リポジトリ名・Worker 名・D1 名・GitHub URL の本筋整合は良好。**齟齬2件発見＋修正**: `src/discord/rest.ts:7` と `scripts/register-commands.js:48` の Discord API `USER_AGENT` で GitHub URL のオーナー名抜け（`https://github.com/discord-event-bot` → `https://github.com/taki98029/discord-event-bot`）＋古い version（`7.0.0` → `7.1.0`）。修正済み。 | 問題なければクローズ |

---

## 3. parity 完成（②検証 / ③本番 を Workers Builds へ）

| ID | 状態 | 担当 | タスク | 依存・備考 |
|----|------|------|--------|-----------|
| P-1 | ✅ | あなた | **②検証(staging) 構築済み**: Worker `discord-event-bot-staging` を Workers Builds で `staging` に接続（Branch control=staging固定・非本番ビルド無効）。D1 `choiemu-event-bot-db-staging` 自動生成。残: テスト用Discordアプリ＋シークレット（→P-4/P-6） | ADR 0012 |
| P-2 | ✅ | 共同 | **完了（2026-06-20）**: 本番 Worker `discord-event-bot` を Cloudflare Workers Builds で `taki98029/event-master-bot` の main に接続成功。接続直後の Bindings タブで **D1 binding UUID = `f6ff753c-bf29-4eb5-83ec-317631743cfc`（本番既存 D1 と完全一致）** を確認＝本番データを保持して移行成功。Cloudflare の auto-provisioning が「同名既存 D1 を再利用」する挙動を実機証明（[ADR 0011 追補2](adr/0011-distribution-and-update-model.md)）。事前に本番 D1 を `.backups/prod-d1-full-2026-06-20.sql` にバックアップ取得済み | ADR 0012 |
| P-3 | ✅ | あなた | **完了（2026-06-20）**: Workers Builds の Deploy command を `npm run deploy` に上書き。②staging（`discord-event-bot-staging`）・③本番（`discord-event-bot`）ともに設定済み。 | distribution-and-environments.html リスク②。確定事項 |
| P-4 | ✅ | あなた | **完了（2026-06-20）**: ②③用シークレット4つをダッシュボードに投入。**②staging**: テスト Discord アプリの4シークレット（PUBLIC_KEY/APP_ID/BOT_TOKEN/ADMIN_TOKEN）を `discord-event-bot-staging` に投入＆再デプロイで反映済み。**③prod**: 旧 `deploy:cli` 時代から本番 Worker `discord-event-bot` に既存シークレットが残置されたまま Workers Builds 接続に引き継がれた（再投入不要）。 | → P-1/P-2 |
| P-5 | 🔲 | CC | 本番が Workers Builds に乗ったら **`deploy:cli` と `wrangler.local.jsonc` を撤去**（移行措置の終了） | → P-2 完了後 |
| P-6 | ✅ | あなた | **完了（2026-06-20）**: テスト用 Discord アプリ作成（Bot/OAuth2 招待/Privileged Intents=SERVER MEMBERS ON/Install Link=None）、Interaction Endpoint URL `https://discord-event-bot-staging.cloudflare-server.workers.dev/interactions` を Discord 側に Save（緑チェック）→ 管理 UI から「コマンドを登録」成功＝Discord 上で動作確認済み | → P-1 |

---

## 4. 実デプロイ後の検証・記録更新

| ID | 状態 | 担当 | タスク | 依存・備考 |
|----|------|------|--------|-----------|
| V-1 | ✅ | CC | **完了（2026-06-20）**: Workflow 並列で ADR 0011（追補2＝同名既存 D1 再利用の実機検証）/ ADR 0012（採用・実装完了へステータス更新）/ dev-and-release-flow.md の「未検証/移行中」記述を「検証済み・完了」へ整合済み。 | → B-1 or P-1 |
| V-2 | ✅ | CC | **完了（2026-06-20）**: distribution-and-environments.html §4 リスク② の badge を `b-high`→`b-ok`、見出しを「最優先」→「対応済み」へ更新。②③とも Deploy command を `npm run deploy` に上書き設定済みを明記。 | → P-3 |
| V-3 | ✅ | CC | **完了（2026-06-20）**: distribution-and-environments.html §4 リスク① の「残課題」を「クローズ（2026-06-20）」へ書き換え。staging 実機検証＋本番接続で同名既存 D1 再利用挙動を確認し、本番データ保持で移行完了を記録。 | → B-1 |

---

## 5. 運用・品質

| ID | 状態 | 担当 | タスク | 依存・備考 |
|----|------|------|--------|-----------|
| Q-1 | 🔲 | あなた | **DB変更ありリリース前の D1 バックアップ運用**を決める／手順化（`wrangler d1 export` 等） | parity でも本番データは保護したい |
| Q-2 | 📝 | CC | **注意事項（記録）**: `package.json` を変更して `npm install` する際は **npm10** で（`@emnapi` ピン留め＝Deployビルド対策を崩さないため）。wrangler floor は `^4.102.0` 固定 | 既知の落とし穴。lockfileは手修正済み |
| Q-3 | ❓ | あなた | Discord **Server Members Intent**（特権）有効化が各利用者で必要 → setup.html の案内が十分か確認 | 既存機能の前提 |

---

## 6. 低優先 / 将来（要精査）

> 以前の検討で「条件付き・低優先」として保留した項目。詳細は再精査が必要（プレースホルダ）。

| ID | 状態 | 担当 | タスク | 依存・備考 |
|----|------|------|--------|-----------|
| L-1 | ⏸ | CC | RRULE フォールバックの拡充（旧 #11） | 要件再確認 |
| L-2 | ⏸ | CC | 管理UIビルダーの拡張（旧 #12） | 要件再確認 |
| L-3 | ⏸ | CC | `mention_enabled` 周りの整理・クリーンアップ（旧 #13） | 影響範囲調査 |

---

## 7. 直近の次アクション（おすすめ順）

> ★ 主要マイルストーン B-0〜B-5/B-7・R-1〜R-6・P-1〜P-4/P-6・V-1〜V-3 は完了。残るは以下のみ。

1. **動作観察期間（1〜2週間）**: 本番 Workers Builds 経路で安定稼働するかを観察。Sync fork ボタンの利用者体験は R-3 公開後の自然な機会で確認。
2. **P-5**（CC）: 安定観察後、`deploy:cli` と `wrangler.local.jsonc` を撤去。`.claude/rules/dev-and-release.md`・dev-and-release-flow.md の該当記述も削除。
3. **B-6 残り**（CC）: distribution-and-environments.html §2/§3 の図表全面改訂（運用支障無し・優先低）。
4. **Q-1**（あなた）: DB 変更ありリリース前の D1 バックアップ運用を体系化（`.backups/prod-d1-full-*.sql` 形式の手順を確立）。

---

## 8. オープンな決定事項

| ID | 状態 | 論点 | 備考 |
|----|------|------|------|
| D-1 | ✅ | **B-3 の着手順** → 「先にCCが公式手順ベースで下書き → 実機(B-1)で画面ラベルを微修正」に決定（2026-06-20）。設計上の成立は B-0 で検証済みのため先行実装した | 残るは B-1 でのラベル確認のみ |
| D-2 | ✅ | 配布・更新の主経路 → **案B（Fork＋Workers Builds＋Sync fork）に決定** | 2026-06-20 |
| D-3 | ✅ | 3層 parity を採用方針に → **ADR 0012 で決定** | 2026-06-20 |

---

## 9. 完了済み（記録）

- ✅ **`fb97ff5`**: 配布用 `wrangler.jsonc` 化＋`database_id` 空化、`wrangler.local.jsonc`＋`deploy:cli` 移行措置、`wrangler` floor `^4.102.0`、3層 parity を [ADR 0012](adr/0012-three-tier-parity.md) として採用・[ADR 0008](adr/0008-verify-on-deployed-test-worker.md) 追補、リスク①対応。ローカル全87テスト＋typecheck 通過。**（現ブランチのみ・未公開 → R-1〜R-3 で反映）**
- ✅ **`31d4964`**: 必要アカウントを「Discord/Cloudflare/GitHub の3つ」に統一、[distribution-and-environments.html](distribution-and-environments.html) 新規作成。
- ✅ **B-0**: 案Bの文書上の成立を一次情報で検証。
- ✅ **B-3 / B-4 / B-5（2026-06-20）**: 案Bのドキュメント実装。setup.html STEP 3 を Fork→接続 に書き換え、README・dev-and-release-flow.md・.claude/rules・ADR 0012・wrangler.jsonc・.dev.vars.example の Deploy ボタン参照を案Bへ整合、ADR 0011 に却下理由を追補。**未コミット**（R-1 で main へ）。
- ✅ 旧Vercel＋Google Sheets版の撤去（本番カットオーバー 2026-06-19 完了）。
- ✅ **`857879e` / `a6234f1`（2026-06-20）**: スラッシュコマンド再編。`/assign` `/pause` `/resume` `/members` を削除（管理 UI で代替可・特に `/members` 全件版は N+1 クエリで Discord 3秒タイムアウトを超えるバグも内包していた）し、`/recruit`（即時募集・Discord 内で叩く意義あり）のみ残置。新規 `/help`（誰でも実行可・コマンド一覧＋管理画面URLを ephemeral 表示）と `/manage`（管理者用・管理画面URLのみ ephemeral 表示）を追加。`handleInteraction` で Worker の origin を動的取得して新ハンドラーに渡す。main・public/main 両方に反映済み。typecheck・vitest 全 87 テスト通過。**反映には Discord 本番アプリ管理画面の「コマンドを登録」ボタンを 1 回押してコマンド一覧を Discord 側と同期する作業が別途必要**（commands.json と Discord 側コマンド一覧の同期）。
- ✅ **B-6 完遂（2026-06-20）**: distribution-and-environments.html §1〜§5 を案B 整合へ全面改訂＋全文書間の整合性監査で 19 件の不整合を修正（README/setup.html/ui/index.html/src/interactions/index.ts/dev-and-release-flow.md/IMPLEMENTATION-CONTRACT.md/ADR 0009/distribution-and-environments.html）。Workflow 5 並列で監査実施（[setup.html 精密検証 / §2/§3 改訂方針策定 / 全文書間整合性 / 実装vs文書 / 直近変更波及]）。typecheck・vitest 全 87 テスト通過。
